/**
 * @deprecated The legacy client-side 500-row/detail-hydration projection was
 * removed. Barcode label rows now come from the tenant-bound server projection.
 */
export const LEGACY_BARCODE_LABEL_PROJECTION_REMOVED = true as const;
