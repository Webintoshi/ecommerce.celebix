import type { AnalyticsMetricResult,AnalyticsMetricType,AnalyticsRange,AnalyticsSummary } from "@celebix/saas-contracts";import type { UmamiPrivateApiConfig } from "./config.ts";import { inputHostname,inputName,inputUuid,parseLogin,parseMetrics,parseSummaryParts,parseWebsite,type UmamiWebsite } from "./parsers.ts";import { readUmamiJson } from "./response.ts";
export type UmamiFetch=(request:Request)=>Promise<Response>;export class UmamiProviderError extends Error{constructor(readonly code:"umami_provider_input_invalid"|"umami_provider_timeout"|"umami_provider_response_invalid"|"umami_provider_response_too_large"|"umami_provider_unavailable"){super(code);this.name="UmamiProviderError"}}
export interface UmamiClient{createWebsite(input:Readonly<{websiteId:string;name:string;domain:string}>):Promise<UmamiWebsite>;getWebsite(websiteId:string):Promise<UmamiWebsite|null>;summary(input:Readonly<{websiteId:string;range:AnalyticsRange;timezone:string;now:Date}>):Promise<AnalyticsSummary>;metrics(input:Readonly<{websiteId:string;range:AnalyticsRange;timezone:string;type:AnalyticsMetricType;now:Date}>):Promise<AnalyticsMetricResult>}
type Audit=(event:Readonly<{operation:string;outcome:"completed"|"rejected"|"unavailable"}>)=>void|Promise<void>;function provider(error:unknown):UmamiProviderError{if(error instanceof UmamiProviderError)return error;if(error instanceof Error&&["umami_provider_input_invalid","umami_provider_timeout","umami_provider_response_invalid","umami_provider_response_too_large","umami_provider_unavailable"].includes(error.message))return new UmamiProviderError(error.message as UmamiProviderError["code"]);return new UmamiProviderError("umami_provider_unavailable")}function stableDate(value:unknown){if(!(value instanceof Date)||!Number.isFinite(value.getTime()))throw new UmamiProviderError("umami_provider_input_invalid");return new Date(value)}function timezone(value:unknown){if(typeof value!=="string"||value.length<1||value.length>100||value!==value.trim())throw new UmamiProviderError("umami_provider_input_invalid");try{new Intl.DateTimeFormat("en",{timeZone:value})}catch{throw new UmamiProviderError("umami_provider_input_invalid")}return value}function range(value:unknown):AnalyticsRange{if(value!=="7d"&&value!=="30d"&&value!=="90d")throw new UmamiProviderError("umami_provider_input_invalid");return value}function metric(value:unknown):AnalyticsMetricType{if(!["path","referrer","device","country"].includes(String(value)))throw new UmamiProviderError("umami_provider_input_invalid");return value as AnalyticsMetricType}
class Semaphore{private active=0;private queue:Array<()=>void>=[];async run<T>(work:()=>Promise<T>):Promise<T>{if(this.active>=4)await new Promise<void>((resolve)=>this.queue.push(resolve));this.active++;try{return await work()}finally{this.active--;this.queue.shift()?.()}}}
export function createUmamiClient(config:UmamiPrivateApiConfig,dependencies:{fetch:UmamiFetch;audit?:Audit}):UmamiClient{if(!config||config.mode!=="approved_staging"||typeof dependencies?.fetch!=="function")throw new UmamiProviderError("umami_provider_unavailable");const selected=Object.freeze({...config}),fetcher=dependencies.fetch,audit=dependencies.audit??(()=>undefined),semaphore=new Semaphore();let token:string|null=null,loginPromise:Promise<string>|null=null;function observe(operation:string,outcome:"completed"|"rejected"|"unavailable"){try{void Promise.resolve(audit(Object.freeze({operation,outcome}))).catch(()=>undefined)}catch{}}
  async function raw(method:string,path:string,body:unknown,authorization?:string){return semaphore.run(async()=>{const controller=new AbortController();let timer:ReturnType<typeof setTimeout>|undefined;try{const headers=new Headers({accept:"application/json"});if(authorization)headers.set("authorization",`Bearer ${authorization}`);let encoded:string|undefined;if(body!==undefined){encoded=JSON.stringify(body);headers.set("content-type","application/json")}const request=new Request(`${selected.apiBaseUrl}${path}`,{method,headers,body:encoded,redirect:"manual",cache:"no-store",signal:controller.signal});const timeout=new Promise<never>((_resolve,reject)=>{timer=setTimeout(()=>{controller.abort();reject(new UmamiProviderError("umami_provider_timeout"))},selected.timeoutMs)});return await Promise.race([fetcher(request),timeout])}catch(error){throw provider(error)}finally{if(timer)clearTimeout(timer)}})}
  async function login(){if(token)return token;if(loginPromise)return loginPromise;loginPromise=(async()=>{const response=await raw("POST","/api/auth/login",{username:selected.username,password:selected.password});if(response.status<200||response.status>=300)throw new UmamiProviderError("umami_provider_unavailable");return parseLogin(await readUmamiJson(response,selected.maximumResponseBytes))})().then((value)=>{token=value;return value}).catch((error)=>{throw provider(error)}).finally(()=>{loginPromise=null});return loginPromise}
  async function request(method:string,path:string,body:unknown,retry:boolean,allow404=false):Promise<unknown|null>{const firstToken=await login();let response=await raw(method,path,body,firstToken);if(allow404&&response.status===404)return null;if(response.status===401&&retry){if(token===firstToken)token=null;const next=await login();response=await raw(method,path,body,next);if(allow404&&response.status===404)return null}if(response.status<200||response.status>=300)throw new UmamiProviderError("umami_provider_unavailable");return readUmamiJson(response,selected.maximumResponseBytes)}
  function params(input:{range:AnalyticsRange;timezone:string;now:Date}){const days={"7d":7,"30d":30,"90d":90}[input.range],end=input.now.getTime(),start=end-days*86400000;return new URLSearchParams({startAt:String(start),endAt:String(end),unit:"day",timezone:input.timezone}).toString()}
  const client:UmamiClient={async createWebsite(input:Readonly<{websiteId:string;name:string;domain:string}>){try{const value=parseWebsite(await request("POST","/api/websites",{id:inputUuid(input?.websiteId),name:inputName(input?.name),domain:inputHostname(input?.domain)},false));observe("create_website","completed");return value}catch(error){const safe=provider(error);observe("create_website",safe.code.includes("input")||safe.code.includes("response")?"rejected":"unavailable");throw safe}},async getWebsite(websiteId:string){try{const id=inputUuid(websiteId),value=await request("GET",`/api/websites/${id}`,undefined,true,true);if(value===null)return null;const parsed=parseWebsite(value);if(parsed.id!==id)throw new UmamiProviderError("umami_provider_response_invalid");return parsed}catch(error){throw provider(error)}},async summary(input:Readonly<{websiteId:string;range:AnalyticsRange;timezone:string;now:Date}>){try{const id=inputUuid(input?.websiteId),selectedRange=range(input?.range),zone=timezone(input?.timezone),now=stableDate(input?.now),query=params({range:selectedRange,timezone:zone,now});const[stats,series,active]=await Promise.all([request("GET",`/api/websites/${id}/stats?${query}`,undefined,true),request("GET",`/api/websites/${id}/pageviews?${query}`,undefined,true),request("GET",`/api/websites/${id}/active`,undefined,true)]);return parseSummaryParts(stats,series,active,{range:selectedRange,now})}catch(error){throw provider(error)}},async metrics(input:Readonly<{websiteId:string;range:AnalyticsRange;timezone:string;type:AnalyticsMetricType;now:Date}>){try{const id=inputUuid(input?.websiteId),selectedRange=range(input?.range),zone=timezone(input?.timezone),selectedType=metric(input?.type),now=stableDate(input?.now),query=params({range:selectedRange,timezone:zone,now});const value=await request("GET",`/api/websites/${id}/metrics?${query}&type=${selectedType}`,undefined,true);return parseMetrics(value,{range:selectedRange,type:selectedType,now,maximumRows:selected.maximumMetricRows})}catch(error){throw provider(error)}}};return Object.freeze(client)
}
