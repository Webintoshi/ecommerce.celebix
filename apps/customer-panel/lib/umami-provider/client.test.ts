import assert from "node:assert/strict";import test from "node:test";import { createUmamiClient,UmamiProviderError,type UmamiFetch } from "./client.ts";import type { UmamiPrivateApiConfig } from "./config.ts";
const WEBSITE="50000000-0000-4000-8000-000000000001",NOW=new Date("2026-07-26T12:00:00.000Z"),CONFIG:UmamiPrivateApiConfig=Object.freeze({mode:"approved_staging",apiBaseUrl:"https://analytics.example.test",username:"admin",password:"private-password",timeoutMs:3000,maximumResponseBytes:262144,maximumMetricRows:100});
function response(status:number,value:unknown,headers:HeadersInit={"content-type":"application/json"}){return new Response(value===undefined?undefined:JSON.stringify(value),{status,headers})}function login(){return response(200,{token:"bearer-token"})}function site(){return{id:WEBSITE,name:"Celebix Store",domain:"store.example.test"}}function officialWebsite(){return{...site(),resetAt:null,userId:"60000000-0000-4000-8000-000000000001",teamId:null,createdBy:"60000000-0000-4000-8000-000000000001",createdAt:"2026-07-26T12:00:00.000Z",updatedAt:"2026-07-26T12:00:00.000Z",deletedAt:null,replayEnabled:false,replayConfig:null,shareId:null}}
function fixture(values:Array<Response|((request:Request)=>Response|Promise<Response>)>){const requests:Request[]=[];const fetch:UmamiFetch=async(request)=>{requests.push(request.clone());const value=values.shift();if(!value)throw Error("missing");return typeof value==="function"?value(request):value};return{client:createUmamiClient(CONFIG,{fetch}),requests}}
test("login bearer use and exact v3 create body",async()=>{const f=fixture([login(),response(200,site())]);assert.deepEqual(await f.client.createWebsite({websiteId:WEBSITE,name:"Celebix Store",domain:"store.example.test"}),site());assert.equal(f.requests[0]?.url,"https://analytics.example.test/api/auth/login");assert.deepEqual(JSON.parse(await f.requests[0]!.text()),{username:"admin",password:"private-password"});assert.equal(f.requests[1]?.headers.get("authorization"),"Bearer bearer-token");assert.deepEqual(JSON.parse(await f.requests[1]!.text()),{id:WEBSITE,name:"Celebix Store",domain:"store.example.test"})});
test("official Umami 3.1 website metadata projects only the trusted identity fields",async()=>{const value=await fixture([login(),response(200,officialWebsite())]).client.getWebsite(WEBSITE);assert.deepEqual(value,site())});
test("unknown Umami website metadata remains fail closed",async()=>{await assert.rejects(()=>fixture([login(),response(200,{...officialWebsite(),unexpected:true})]).client.getWebsite(WEBSITE),/umami_provider_response_invalid/)});
test("one token is cached across reads",async()=>{const f=fixture([login(),response(200,site()),response(200,site())]);await f.client.getWebsite(WEBSITE);await f.client.getWebsite(WEBSITE);assert.equal(f.requests.filter((r)=>r.url.endsWith("/api/auth/login")).length,1)});
test("read-only 401 relogs and retries once",async()=>{const f=fixture([login(),response(401,{}),login(),response(200,site())]);assert.deepEqual(await f.client.getWebsite(WEBSITE),site());assert.equal(f.requests.filter((r)=>r.url.endsWith(`/api/websites/${WEBSITE}`)).length,2)});
test("write 401 never retries create",async()=>{const f=fixture([login(),response(401,{})]);await assert.rejects(()=>f.client.createWebsite({websiteId:WEBSITE,name:"Celebix Store",domain:"store.example.test"}),/umami_provider_unavailable/);assert.equal(f.requests.filter((r)=>r.method==="POST"&&r.url.endsWith("/api/websites")).length,1)});
test("manual redirects and non-success statuses fail closed",async()=>{for(const value of [response(302,{}, {"content-type":"application/json","location":"https://evil.test"}),response(500,{})]){const f=fixture([login(),value]);await assert.rejects(()=>f.client.getWebsite(WEBSITE),/umami_provider_unavailable/);assert.equal(f.requests[1]?.redirect,"manual")}});
test("content type and declared length are exact and bounded",async()=>{for(const value of [response(200,site(),{"content-type":"text/plain"}),response(200,site(),{"content-type":"application/json","content-length":"262145"})])await assert.rejects(()=>fixture([login(),value]).client.getWebsite(WEBSITE),/umami_provider_response_/)});
test("stream overflow fatal UTF-8 and JSON are rejected",async()=>{const badUtf=new Response(new Uint8Array([0xff]),{status:200,headers:{"content-type":"application/json"}}),badJson=new Response("{",{status:200,headers:{"content-type":"application/json"}}),big=new Response(new Uint8Array(262145),{status:200,headers:{"content-type":"application/json"}});for(const value of [badUtf,badJson,big])await assert.rejects(()=>fixture([login(),value]).client.getWebsite(WEBSITE),/umami_provider_response_/)});
test("get website maps 404 to null and rejects mismatched identities",async()=>{assert.equal(await fixture([login(),response(404,{})]).client.getWebsite(WEBSITE),null);await assert.rejects(()=>fixture([login(),response(200,{...site(),id:"50000000-0000-4000-8000-000000000002"})]).client.getWebsite(WEBSITE),/umami_provider_response_invalid/)});
test("summary merges official Umami 3.1 stats sessions and active visitors",async()=>{const stats={pageviews:10,visitors:6,visits:7,bounces:2,totaltime:140,comparison:{pageviews:1,visitors:1,visits:1,bounces:0,totaltime:20}},series={pageviews:[{x:"2026-07-25T00:00:00.000Z",y:4}],sessions:[{x:"2026-07-25T00:00:00.000Z",y:3}]};const f=fixture([login(),response(200,stats),response(200,series),response(200,{visitors:2})]);const result=await f.client.summary({websiteId:WEBSITE,range:"7d",timezone:"Europe/Istanbul",now:NOW});assert.equal(result.bounceRateBasisPoints,2857);assert.equal(result.averageVisitSeconds,20);assert.equal(result.activeVisitors,2);assert.deepEqual(result.visitsSeries,[{at:"2026-07-25T00:00:00.000Z",value:3}]);assert.equal(Object.isFrozen(result),true)});
test("summary canonicalizes official Umami local bucket timestamps",async()=>{const stats={pageviews:1,visitors:1,visits:1,bounces:1,totaltime:0,comparison:{pageviews:0,visitors:0,visits:0,bounces:0,totaltime:0}},series={pageviews:[{x:"2026-07-27 00:00:00",y:1}],sessions:[{x:"2026-07-27 00:00:00",y:1}]};const result=await fixture([login(),response(200,stats),response(200,series),response(200,{visitors:0})]).client.summary({websiteId:WEBSITE,range:"30d",timezone:"Europe/Istanbul",now:NOW});assert.deepEqual(result.pageviewsSeries,[{at:"2026-07-26T21:00:00.000Z",value:1}]);assert.deepEqual(result.visitsSeries,[{at:"2026-07-26T21:00:00.000Z",value:1}])});
test("metrics parse and sanitize provider rows",async()=>{const f=fixture([login(),response(200,[{x:"/products",y:4},{x:"/",y:2}])]);const result=await f.client.metrics({websiteId:WEBSITE,range:"30d",timezone:"Europe/Istanbul",type:"path",now:NOW});assert.deepEqual(result.items,[{label:"/products",value:4},{label:"/",value:2}]);assert.equal(Object.isFrozen(result.items),true)});
test("request timeout produces only the stable provider error",async()=>{const config={...CONFIG,timeoutMs:5};const client=createUmamiClient(config,{fetch:async()=>new Promise(()=>undefined)});await assert.rejects(()=>client.getWebsite(WEBSITE),(error:unknown)=>error instanceof UmamiProviderError&&error.message==="umami_provider_timeout")});
test("maximum metric rows is enforced",async()=>{const config={...CONFIG,maximumMetricRows:1};await assert.rejects(()=>fixtureWith(config,[login(),response(200,[{x:"/a",y:1},{x:"/b",y:2}])]).client.metrics({websiteId:WEBSITE,range:"7d",timezone:"UTC",type:"path",now:NOW}),/umami_provider_response_invalid/)});
test("errors and audit output never contain credentials or bearer token",async()=>{const audit:unknown[]=[];const client=createUmamiClient(CONFIG,{fetch:async()=>{throw Error("private-password bearer-token")},audit:(event)=>{audit.push(event)}});await assert.rejects(()=>client.getWebsite(WEBSITE),(error:unknown)=>!String(error).includes("private-password"));assert.doesNotMatch(JSON.stringify(audit),/private-password|bearer-token/)});
test("provider concurrency never exceeds four calls",async()=>{let active=0,maximum=0;const fetch:UmamiFetch=async(request)=>{if(request.url.endsWith("/api/auth/login"))return login();active++;maximum=Math.max(maximum,active);await new Promise((resolve)=>setTimeout(resolve,10));active--;return response(200,site())};const client=createUmamiClient(CONFIG,{fetch});await Promise.all(Array.from({length:12},()=>client.getWebsite(WEBSITE)));assert.equal(maximum,4)});
test("all requests are no-store JSON and secret-free inputs reject before fetch",async()=>{const f=fixture([login(),response(200,site())]);await f.client.getWebsite(WEBSITE);for(const request of f.requests){assert.equal(request.cache,"no-store");assert.equal(request.headers.get("accept"),"application/json")}await assert.rejects(()=>f.client.createWebsite({websiteId:WEBSITE,name:" bad",domain:"store.example.test"}),/umami_provider_input_invalid/)});
function fixtureWith(config:UmamiPrivateApiConfig,values:Response[]){const requests:Request[]=[];return{client:createUmamiClient(config,{fetch:async(request)=>{requests.push(request);const value=values.shift();if(!value)throw Error("missing");return value}}),requests}}
