import assert from "node:assert/strict";import test from "node:test";import {createProductPreviewTokenCodec} from "./token.ts";
const binding={storeId:"10000000-0000-4000-8000-000000000001",productId:"20000000-0000-4000-8000-000000000001",principalId:"30000000-0000-4000-8000-000000000001",version:4};const now=new Date("2026-08-30T12:00:00.000Z");
test("preview token is five-minute bound to store product principal and version",()=>{const codec=createProductPreviewTokenCodec({activeKeyId:"active",keys:new Map([["active",new Uint8Array(32).fill(7)]])});const token=codec.issue(binding,now);assert.match(token,/^pp1\.active\./);assert.equal(codec.verify(token,binding,new Date(now.getTime()+299000)),true);assert.equal(codec.verify(token,{...binding,version:5},now),false);assert.equal(codec.verify(token,{...binding,principalId:"30000000-0000-4000-8000-000000000002"},now),false);assert.equal(codec.verify(token,binding,new Date(now.getTime()+300000)),false);});
test("preview token rejects short keys and TTL beyond five minutes",()=>{assert.throws(()=>createProductPreviewTokenCodec({activeKeyId:"active",keys:new Map([["active",new Uint8Array(31)]])}));assert.throws(()=>createProductPreviewTokenCodec({activeKeyId:"active",keys:new Map([["active",new Uint8Array(32)] ]),ttlSeconds:301}));});
