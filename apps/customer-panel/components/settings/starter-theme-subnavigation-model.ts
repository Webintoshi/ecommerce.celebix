export type ThemePanelKey = "visual" | "navigation" | "home" | "product" | "cart" | "footer";

export const THEME_PANEL_KEYS = Object.freeze<readonly ThemePanelKey[]>([
  "visual",
  "navigation",
  "home",
  "product",
  "cart",
  "footer",
]);
