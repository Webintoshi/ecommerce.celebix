"use client";
import { useCallback, useEffect, useState } from "react";
import type { ProductReview } from "@celebix/saas-contracts";
import { PanelEmptyState, PanelPageHeader, PanelPageShell } from "@/components/panel/PanelPageShell";
import { CatalogAdminApiError, catalogAdminApi } from "@/lib/catalog-admin-ui/client";
import styles from "./catalog-admin-console.module.css";
export function ProductReviewConsole({ canModerate }: { canModerate: boolean }) { const [items, setItems] = useState<readonly ProductReview[]>([]), [loading, setLoading] = useState(true), [busy, setBusy] = useState(""), [error, setError] = useState(""); const load = useCallback(async () => { setLoading(true); try { setItems(await catalogAdminApi.reviews()); } catch (caught) { setError(caught instanceof CatalogAdminApiError ? caught.message : "Yorumlar yüklenemedi."); } finally { setLoading(false); } }, []); useEffect(() => { void load(); }, [load]); async function moderate(review: ProductReview, status: "approved" | "rejected" | "archived") { const reply = (document.getElementById(`reply-${review.id}`) as HTMLTextAreaElement | null)?.value.trim(); setBusy(review.id); setError(""); try { await catalogAdminApi.moderateReview(review.id, { expectedVersion: review.version, status, ...(reply ? { reply } : {}) }); await load(); } catch (caught) { setError(caught instanceof CatalogAdminApiError ? caught.message : "Yorum güncellenemedi."); } finally { setBusy(""); } } return <PanelPageShell><PanelPageHeader title="Yorumlar" description="Ürün yorumlarını inceleyin, yanıtlayın ve yayın durumunu yönetin." /><section className={styles.surface}>{error ? <p className={styles.error} role="alert">{error}</p> : null}{loading ? <div className={styles.state} role="status">Yorumlar yükleniyor…</div> : items.length === 0 ? <PanelEmptyState title="Henüz ürün yorumu yok" description="Gerçek bir yorum gönderildiğinde burada görünecek." /> : <div className={styles.list}>{items.map((review) => <article className={`${styles.item} ${styles.review}`} key={review.id}><div><h2>{review.productTitle}</h2><div className={styles.reviewMeta}><span>{"★".repeat(review.rating)}{"☆".repeat(5 - review.rating)}</span><span>{review.reviewerName}</span><span>{review.status}</span></div><p>{review.title ? <strong>{review.title}: </strong> : null}{review.body}</p>{review.merchantReply ? <small>Mağaza yanıtı: {review.merchantReply}</small> : null}</div>{canModerate ? <><textarea id={`reply-${review.id}`} aria-label={`${review.productTitle} mağaza yanıtı`} defaultValue={review.merchantReply} maxLength={2000} placeholder="Mağaza yanıtı (opsiyonel)" /><div className={styles.actions}><button className={styles.primary} disabled={busy === review.id} onClick={() => void moderate(review, "approved")}>Yayınla</button><button className={styles.button} disabled={busy === review.id} onClick={() => void moderate(review, "rejected")}>Reddet</button><button className={styles.danger} disabled={busy === review.id} onClick={() => void moderate(review, "archived")}>Arşivle</button></div></> : null}</article>)}</div>}</section></PanelPageShell>; }
