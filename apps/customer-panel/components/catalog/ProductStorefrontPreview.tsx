"use client";
import {useEffect,useState} from "react";
import type {CatalogProductPreviewProjection} from "@celebix/saas-data";
export function ProductStorefrontPreview({productId,token}:{productId:string;token:string}){const [preview,setPreview]=useState<CatalogProductPreviewProjection>();const [error,setError]=useState("");useEffect(()=>{const controller=new AbortController();fetch(`/api/catalog/products/${productId}/preview?token=${encodeURIComponent(token)}`,{credentials:"same-origin",cache:"no-store",signal:controller.signal}).then(async(response)=>{const body=await response.json();if(!response.ok||!body.preview)throw new Error();setPreview(body.preview);}).catch(()=>{if(!controller.signal.aborted)setError("Önizleme süresi doldu, ürün değişti veya bu oturum için geçerli değil.");});return()=>controller.abort();},[productId,token]);if(error)return <div className="feedback feedback-error" role="alert"><strong>Önizleme açılamadı</strong><p>{error}</p></div>;if(!preview)return <div className="catalog-loading" role="status"><span className="spinner"/> Mağaza önizlemesi hazırlanıyor…</div>;const first=preview.variants[0];return <article className="storefront-product-preview"><div className="product-media-grid">{preview.media.map((item,index)=><img key={item.publicUrl} src={item.publicUrl} alt={item.altText||`${preview.product.title} görsel ${index+1}`}/>)}</div><span className="eyebrow">MAĞAZA ÖNİZLEMESİ</span><h1>{preview.merchandising.seoTitle||preview.product.title}</h1>{preview.product.description?<p>{preview.product.description}</p>:null}<strong>{first?new Intl.NumberFormat("tr-TR",{style:"currency",currency:preview.product.currency}).format(first.priceCents/100):"Fiyat henüz eklenmedi"}</strong>{preview.variants.length?<ul>{preview.variants.map((variant,index)=><li key={`${variant.title}-${index}`}>{variant.title} · {variant.stockTracking?`${variant.stockQuantity} adet`:"Stok takibi yok"}</li>)}</ul>:<p>Satış seçeneği eklenmemiş.</p>}</article>;}
