"use client";
import { useState } from "react";
import { PROMOTION_TEMPLATES } from "@/lib/promotion-ui/model";
import { PromotionList } from "./PromotionList";
import { PromotionEditor } from "./PromotionEditor";
import styles from "./promotion-studio.module.css";
export function LegacyPromotionWarning() { return <section className={styles.warning} role="status"><h1>Eski indirim kaydı</h1><p>Bu kayıt yeni kampanyaya henüz bağlanmadı. Güvenli görüntüleme için destek ekibine başvurun.</p></section>; }
export function PromotionStudio({ mode, timezone, canManage, canPublish, canArchive, promotionId }: Readonly<{ mode: "list" | "create" | "view" | "edit"; timezone: string; canManage: boolean; canPublish: boolean; canArchive: boolean; promotionId?: string }>) { const [template, setTemplate] = useState<typeof PROMOTION_TEMPLATES[number]["id"] | null>(mode === "create" ? null : "custom"); if (mode === "list") return <PromotionList timezone={timezone} canManage={canManage} canPublish={canPublish} canArchive={canArchive} />; if ((mode === "create" || mode === "edit") && !canManage) return <section className={styles.warning} role="status"><h1>Salt okunur erişim</h1><p>Bu rol kampanyaları görüntüleyebilir ve test edebilir; taslak oluşturamaz veya düzenleyemez.</p><a href={promotionId ? `/discounts/${promotionId}` : "/discounts"}>Kampanyalara dön</a></section>; if (mode === "view" && !promotionId) return <LegacyPromotionWarning />; if (template === null) return <section className={styles.templates}><header><span className={styles.eyebrow}>Yeni kampanya</span><h1>Ne yapmak istiyorsunuz?</h1><p>Hazır bir başlangıç seçin; bütün ayrıntıları sonraki beş adımda değiştirebilirsiniz.</p></header><div>{PROMOTION_TEMPLATES.length === 12 && PROMOTION_TEMPLATES.map((item) => <button key={item.id} type="button" onClick={() => setTemplate(item.id)}><strong>{item.title}</strong><span>{item.help}</span><em>Bu şablonla başla →</em></button>)}</div></section>; return <PromotionEditor templateId={template} promotionId={promotionId} timezone={timezone} canManage={canManage} canPublish={canPublish} canArchive={canArchive} readOnly={mode === "view"} />; }
