import { renderMerchantRecordPage } from "@/components/merchant-admin/render-merchant-record-page";
export default async function EditInvoiceIntegrationPage({ params }: { params: Promise<{ recordId: string }> }) { const { recordId } = await params; return renderMerchantRecordPage({ kind: "invoice_integration", permission: "integrations.manage", recordId, returnTo: "/accounting/invoicing-integration" }); }
