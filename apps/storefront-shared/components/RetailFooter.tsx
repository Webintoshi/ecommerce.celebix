import type { PublicStarterThemePresentationV3, PublicStorefront } from "@celebix/saas-contracts";
import Link from "next/link";

import { NewsletterForm } from "./NewsletterForm";

export function RetailFooter({ presentation, storefront }: Readonly<{ presentation: PublicStarterThemePresentationV3; storefront: PublicStorefront }>) {
  return <footer className="store-footer retail-footer" data-footer-tone={presentation.footer.tone}><div className="store-container retail-footer-grid"><div className="retail-footer-brand"><strong>{presentation.displayName}</strong><p>{storefront.hostname}</p>{presentation.supportEmail ? <a href={`mailto:${presentation.supportEmail}`}>{presentation.supportEmail}</a> : null}</div>{presentation.footer.groups.map((group) => <nav aria-label={group.heading} key={group.heading}><span>{group.heading}</span>{group.links.map((link) => <Link href={link.destination} key={`${link.destination}-${link.label}`}>{link.label}</Link>)}</nav>)}{presentation.footer.newsletter.enabled ? <section className="retail-footer-newsletter" aria-labelledby="retail-newsletter-heading"><h2 id="retail-newsletter-heading">{presentation.footer.newsletter.heading}</h2><p>{presentation.footer.newsletter.body}</p><NewsletterForm consentLabel={presentation.footer.newsletter.consentLabel} /></section> : null}</div>{presentation.footer.social.length ? <nav className="store-container retail-footer-social" aria-label="Sosyal medya">{presentation.footer.social.map((item) => <a href={item.url} key={item.network} rel="noopener noreferrer">{item.network}</a>)}</nav> : null}<div className="store-container footer-bottom"><span>© {new Date().getUTCFullYear()} {presentation.displayName}</span><span>TRY · Türkçe</span></div></footer>;
}
